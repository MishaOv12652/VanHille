{
  "name": "vanhilleapp",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "postinstall": "cd angular-src && npm ci --legacy-peer-deps --include=dev && npx ng build && cd ../backend && npm ci --include=dev && npm run build",
    "start": "node backend/dist/main.js",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run start:dev --prefix backend\" \"npm start --prefix angular-src\""
  },
  "author": "Michael Ovodenko",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
